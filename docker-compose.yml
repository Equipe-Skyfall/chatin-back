services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/srv/app:z
      - ./migrations:/srv/migrations:z
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
